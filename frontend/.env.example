# Where the Flask API (server.py) lives. Defaults to the local dev server.
MEALSCOUNT_API_ORIGIN=http://127.0.0.1:5000
# To develop the UI without running Python at all, point at production:
# MEALSCOUNT_API_ORIGIN=https://www.mealscount.com
